# ATLAS Project Dependencies

# Core dependencies
torch>=2.0.0
nvidia-ml-py>=12.535.0  # Replaces deprecated pynvml
numpy>=1.24.0
scikit-learn>=1.3.0

# Phase 3: LLM Integration
transformers>=4.30.0  # HuggingFace transformers for GPT-2, LLaMA, BERT
tokenizers>=0.13.0  # Fast tokenization
datasets>=2.14.0  # HuggingFace datasets (GLUE, etc.)
accelerate>=0.20.0  # Training acceleration

# Phase 4: Graph and Network
networkx>=3.0  # Graph operations for Laplacian

# Visualization
matplotlib>=3.7.0
seaborn>=0.12.0
pandas>=2.0.0  # Data manipulation for results

# System metrics
psutil>=5.9.0  # Memory and CPU monitoring

# Optional: For future phases
peft>=0.4.0  # Parameter-efficient fine-tuning (LoRA)
# ray>=2.5.0  # For distributed simulation (Phase 3+)

# Development tools
pytest>=7.4.0
jupyter>=1.0.0
ipykernel>=6.25.0
ipywidgets>=8.0.0  # Interactive widgets for notebooks
